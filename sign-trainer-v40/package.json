{
  "name": "sign-trainer",
  "version": "0.9.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check src/signs.js && node --check public/app.js"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
